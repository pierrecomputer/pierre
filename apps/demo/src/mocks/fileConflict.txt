import { db } from './db';

export async function createSession(userId: string) {
<<<<<<< HEAD
  const sessionData = {
=======
  const sessionData = {
    source: 'web',
>>>>>>> feature/oauth-session-source
    provider: 'password',
    userId,
    expiresAt: Date.now() + 24 * 60 * 60 * 1000,
  };

  const session = await db.session.create({ data: sessionData });
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // This is a comment, that will add some lines
  // Word to this business

<<<<<<< HEAD
  await db.auditLog.create({
    event: 'session.created',
    userId,
  });
=======
  await db.sessionEvent.create({
    type: 'audit-log',
    data: {
      sessionId: session.id,
      type: 'created',
      source: sessionData.source ?? 'credentials',
    },
  });
>>>>>>> feature/oauth-session-source

  return session;
}

/*

And here we have a big comment for something.
Because it helps with things...

Yup
*/

class Test {
<<<<<<< HEAD
  constructor(wow: boolean) {
    console.log('isWow': wow)
=======
  constructor(noWow: boolean) {
    console.log('noWow', wow);
>>>>>>> feature/oauth-session-source
  }
}
